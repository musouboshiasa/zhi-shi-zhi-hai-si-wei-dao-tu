# The Sea of Knowledge — 使用与迁移指南

本指南回答三个最常见的问题：

1. [关机后如何重新打开软件？](#一关机后如何重新打开软件)
2. [如何把软件移动到其他文件夹/电脑？](#二如何将软件移动或复制到其他电脑)
3. [如何在其他电脑上打开软件？](#二如何将软件移动或复制到其他电脑)

---

## 一、关机后如何重新打开软件？

你的电脑关机后，软件会自动关闭。重新打开只需两步：

### 方法一：双击 bat 文件（最简单 ✅ 推荐）

1. 打开文件夹 **`the sea of knowledge`**
2. 找到文件 **`启动软件.bat`**
3. **双击它**，软件就会自动启动
4. 浏览器会自动打开 http://localhost:3000

> 📌 把 `启动软件.bat` 发送到桌面快捷方式会更方便，见下方【创建桌面快捷方式】。

### 方法二：手动启动

1. 按 `Win + R`，输入 `cmd`，回车
2. 输入以下命令（复制粘贴）：
   ```
   cd "C:\Users\mosouboshiasa\WorkBuddy\the sea of knowledge"
   node server.js
   ```
3. 回车
4. 打开浏览器，访问 http://localhost:3000

---

## 二、如何将软件移动或复制到其他电脑？

软件是 **完全便携的**——整个 `the sea of knowledge` 文件夹就是软件的完整内容。

### 步骤一：打包当前软件

**在当前电脑上：**

1. 打开文件夹 `C:\Users\mosouboshiasa\WorkBuddy\`
2. 找到 `the sea of knowledge` 文件夹
3. 右键 → **发送到** → **压缩(zipped)文件夹**
4. 把生成的 `.zip` 文件复制到 U盘 / 网盘 / 微信

### 步骤二：在新电脑上部署

**在新电脑上：**

#### 第1步：安装 Node.js（只需做一次）

1. 打开浏览器，访问：**https://nodejs.org**
2. 点击左边的 **LTS** 按钮（绿色大按钮），下载安装包
3. 双击下载的文件，一路点 **Next**，全部默认即可
4. 安装完成后，**重启电脑**

![](说明：访问 nodejs.org 下载 LTS 版本)

#### 第2步：解压软件

1. 把 zip 文件复制到新电脑上任意位置（比如桌面或 D 盘）
2. 右键 zip 文件 → **全部解压缩**
3. 解压后会得到 `the sea of knowledge` 文件夹

#### 第3步：启动软件

进入解压后的文件夹，**双击 `启动软件.bat`** 即可。

> 首次启动会自动安装依赖（需要联网，大约1分钟），之后启动就很快了。

---

## 三、创建桌面快捷方式（让打开更方便）

### Windows 桌面快捷方式

1. 打开 `the sea of knowledge` 文件夹
2. 找到 `启动软件.bat` 文件
3. **右键** → **发送到** → **桌面快捷方式**
4. 桌面上会出现一个新的快捷方式图标

以后每次开机，**双击桌面上的快捷方式** 就能启动软件！

### 设置开机自动启动（可选）

如果想开机后软件自动启动：

1. 按 `Win + R`，输入 `shell:startup`，回车
2. 会打开一个文件夹（这是 Windows 的"启动"文件夹）
3. 把桌面上的 `启动软件.bat - 快捷方式` **拖进**这个文件夹
4. 完成！以后每次开机，软件自动启动

---

## 四、数据备份与恢复

你的知识点和图片都保存在 `知识点库` 文件夹中。

### 手动备份

直接复制整个 `知识点库` 文件夹到安全位置（如 U盘、云盘）即可。

### 云端同步（推荐）

使用软件内置的云端同步功能：

1. 按照 `云端服务器搭建指南（阿里云）.md` 搭建服务器
2. 在软件设置中配置云端信息
3. 点击"上传到云端"备份，点击"从云端下载"恢复

这样即使电脑坏了，知识点也不会丢！

---

## 常见问题

### Q: 启动后浏览器没有自动打开？
手动打开浏览器，在地址栏输入 `http://localhost:3000` 回车。

### Q: 显示"端口被占用"？
说明软件已经在运行了。直接打开 http://localhost:3000 即可。

### Q: 软件窗口不小心关闭了怎么办？
重新双击 `启动软件.bat` 即可。你的知识点数据不会丢失——它们保存在文件夹中。

### Q: 移动软件后知识点还在吗？
在！所有知识点都保存在 `知识点库` 文件夹里。只要整个文件夹一起移动，数据就完整保留。

### Q: 两台电脑如何同步知识点？
使用云端同步功能。在电脑A上传，在电脑B下载，即可保持同步。

### Q: Node.js 是什么？必须安装吗？
Node.js 是软件的运行环境，只需安装一次。它就像一个"翻译官"，让软件能在电脑上运行。安装非常简单，一路点 Next 就行。
