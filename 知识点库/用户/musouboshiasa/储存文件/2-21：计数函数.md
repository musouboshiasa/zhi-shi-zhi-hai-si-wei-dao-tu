（（编号区））
2-21
（（名称区））
计数函数
（（正文区））
包括count函数、countif函数和countifs函数。  
# count函数  
- 它只能支持识别数字单元格。（counta函数支持识别文字单元格）
# countif函数/countifs函数  
countif函数一个变量，countifs函数两个变量。  
第一个框是区域，第二个是条件。
（（前相关区））
- 2-8：包括：EXCEL
（（后相关区））
（（结束））
